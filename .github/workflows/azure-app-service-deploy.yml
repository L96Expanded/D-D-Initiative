name: Deploy to Azure App Service

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '18'
  AZURE_WEBAPP_NAME: 'dnd-initiative-prod'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    # ========================================
    # Backend Build & Test
    # ========================================
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Install backend dependencies
      run: |
        cd backend
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Run backend tests
      run: |
        echo "Skipping tests for now - deploy only"
        # cd backend
        # pytest --maxfail=1 --disable-warnings -q
    
    # ========================================
    # Frontend Build
    # ========================================
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
    
    - name: Install frontend dependencies
      run: |
        cd frontend
        npm install
    
    - name: Build frontend
      run: |
        cd frontend
        npm run build
      env:
        VITE_API_URL: https://karsusinitiative.com
    
    - name: Copy frontend build to backend
      run: |
        rm -rf backend/static
        mkdir -p backend/static
        cp -r frontend/dist/* backend/static/
        echo "Static files copied:"
        ls -la backend/static/
    
    # ========================================
    # Deploy to Azure
    # ========================================
    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    
    - name: Clean deployment cache
      run: |
        az webapp deployment source delete --name ${{ env.AZURE_WEBAPP_NAME }} --resource-group BCSAI2025-DEVOPS-STUDENTS-A || true
    
    - name: Deploy Backend to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        package: ./backend
    
    - name: Configure App Settings
      run: |
        az webapp config appsettings set \
          --name ${{ env.AZURE_WEBAPP_NAME }} \
          --resource-group BCSAI2025-DEVOPS-STUDENTS-A \
          --settings \
            DATABASE_URL="${{ secrets.DATABASE_URL }}" \
            SECRET_KEY="${{ secrets.SECRET_KEY }}" \
            AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
            ENVIRONMENT="production" \
            CORS_ORIGINS='["https://karsusinitiative.com","https://dnd-initiative-prod.azurewebsites.net","http://localhost:3000"]' \
            ALLOWED_HOSTS='["karsusinitiative.com","dnd-initiative-prod.azurewebsites.net","localhost"]' \
            SCM_DO_BUILD_DURING_DEPLOYMENT="false" \
            WEBSITE_HTTPLOGGING_RETENTION_DAYS="3"
    
    - name: Restart App Service
      run: |
        echo "Restarting App Service to apply changes..."
        az webapp restart \
          --name ${{ env.AZURE_WEBAPP_NAME }} \
          --resource-group BCSAI2025-DEVOPS-STUDENTS-A
    
    - name: Test Deployment
      run: |
        echo "Deployment completed. Testing health endpoint..."
        sleep 30
        
        response=$(curl -s -o /dev/null -w "%{http_code}" https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net/api/health || echo "000")
        
        if [ "$response" == "200" ]; then
          echo "✓ Health check passed!"
        else
          echo "⚠ Health check returned HTTP $response (may need manual verification)"
        fi
    
    - name: Display URLs
      run: |
        echo "==================================="
        echo "Deployment Complete!"
        echo "==================================="
        echo "App URL: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net"
        echo "Health: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net/api/health"
        echo "API Docs: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net/docs"
