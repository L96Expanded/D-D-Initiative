# D&D Initiative Tracker - Environment Configuration Template
# Copy this file to .env and update the values below

# ========================================
# DATABASE CONFIGURATION
# ========================================
POSTGRES_DB=dnd_tracker
POSTGRES_USER=dnd_user
# ⚠️ CHANGE THIS! Generate a strong password (20+ characters)
POSTGRES_PASSWORD=changeme_to_secure_password

# ========================================
# JWT AUTHENTICATION
# ========================================
# ⚠️ CHANGE THIS! Generate a long random string (64+ characters)
# You can generate one by running: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=changeme_to_long_random_secret_key
SECRET_KEY=changeme_to_long_random_secret_key
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ========================================
# DATABASE CONNECTION
# ========================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For local Docker: use 'postgres' as host
# For production: use your database server hostname
DATABASE_URL=postgresql://dnd_user:changeme_to_secure_password@postgres:5432/dnd_tracker

# ========================================
# APPLICATION ENVIRONMENT
# ========================================
# Options: development, production
ENVIRONMENT=development
APP_VERSION=1.0.1

# ========================================
# SECURITY SETTINGS
# ========================================
# Set to 'true' in production with HTTPS
SECURE_COOKIES=false

# ========================================
# CORS & ALLOWED HOSTS
# ========================================
# JSON array format required! Keep the quotes and brackets exactly as shown
# For local development, the defaults below should work
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
ALLOWED_HOSTS=["localhost","127.0.0.1"]

# For production, add your domain:
# CORS_ORIGINS=["http://localhost:3000","https://yourdomain.com","http://yourdomain.com"]
# ALLOWED_HOSTS=["localhost","127.0.0.1","yourdomain.com"]

# ========================================
# DOMAIN CONFIGURATION (Production Only)
# ========================================
# Your domain name or external IP for production deployment
# Leave empty for local development
DOMAIN_NAME=

# ========================================
# FILE UPLOAD DIRECTORIES
# ========================================
# Default paths work with Docker - usually no need to change these
UPLOAD_DIR=/app/uploads
DATABASE_IMAGES_DIR=/app/database_images
MAX_FILE_SIZE=10485760

# ========================================
# AZURE STORAGE (Optional - Production Only)
# ========================================
# Only needed if using Azure Blob Storage for images
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER_NAME=creature-images

# ========================================
# FRONTEND API URL
# ========================================
# For local development, use localhost
VITE_API_URL=http://localhost:8000

# For production, use your actual API URL:
# VITE_API_URL=https://api.yourdomain.com
# or
# VITE_API_URL=https://yourdomain.com/api

# ========================================
# SETUP INSTRUCTIONS
# ========================================
# 1. Copy this file: cp .env.example .env (Windows: copy .env.example .env)
# 2. Generate secure passwords and secrets (see comments above)
# 3. Update POSTGRES_PASSWORD and JWT_SECRET
# 4. For production: Update DOMAIN_NAME, CORS_ORIGINS, and ALLOWED_HOSTS
# 5. Save the file and run: docker-compose up --build
#
# Security Tips:
# - Never commit .env to version control (already in .gitignore)
# - Use different secrets for development and production
# - Keep your .env file secure and backed up
# - Rotate secrets periodically in production
